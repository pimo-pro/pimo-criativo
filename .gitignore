# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Segredos locais (chaves API, credenciais) — nunca versionar
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env.staging
.env.staging.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Security keys
deploy_key
deploy_key.pub
.git_disabled/

# Auth PHP (Hostinger) — contém passwordHash
api/data/users.json

# Env SERVER-ONLY (Hostinger Shared) — nunca versionar
**/server-env.local.php
pimo-private/

# Arquivo GitHub de projetos — config com token (nunca versionar)
hostinger/api/projects/githubSyncConfig.php
public_html/api/projects/githubSyncConfig.php

.claude/

# DEV local (@PIMO-SOON) — nunca incluir na publicacao
/dev/
~$*

# Sobras locais de testes e backups (L-28)
tmp/
test-output/

# Outputs locais do smoke TCN (gen-test-tcn) — regeneráveis, não versionar
scripts/armario_*.tcn

# Cache local CLI Supabase — não versionar
supabase/.temp/

# Artefactos locais de agentes / diagnóstico (não versionar)
_tmp_*/
AUDITORIA_TECNICA_INTEGRAL.md
scripts/_tmp_*.mjs

