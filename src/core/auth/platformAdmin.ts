/** Role canónica do administrador único da plataforma (SSOT alinhado com users.json). */
export const PLATFORM_ADMIN_ROLE = "admin";

/** E-mail do administrador único — exceção blindada à verificação de email (case-insensitive). */
export const PLATFORM_ADMIN_EMAIL = "shecivara@gmail.com";
